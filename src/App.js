import React from "react";
import ConfigurationForm from "./components/ConfigurationForm"; // Adjust the path if needed

function App() {
    return (
        <div className="App">
            <ConfigurationForm />
        </div>
    );
}

export default App;
